import 'package:flutter/material.dart';

class TickertScreen extends StatelessWidget {
  const TickertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
            child: Text(
                'THis page is responsible for Generating Qr Code for Tickets and Managing All Tickrts ')));
  }
}
